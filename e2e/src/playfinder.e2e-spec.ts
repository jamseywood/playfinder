import { PlayfinderPage } from "./pages/playfinder.po";


describe('HYHSM account page', () => {
    let accountPage: PlayfinderPage = new PlayfinderPage();

    beforeAll(() => {
        //normally navigation through the application to the page that you want to test
    });

    afterAll(() => {
    });

    it('should have the correct page title', () => {
    });
});
